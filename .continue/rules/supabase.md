# Supabase Rules

- Use @supabase/ssr only
- Do not use deprecated auth helpers unless compatibility requires it
- Separate browser and server clients
- Never expose service_role keys
- Prefer SSR-compatible auth flows
- Keep middleware/proxy compatible with Next.js 16
- Prefer RLS-compatible architecture
